#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;
    int arr[n];

    for (int i = 0; i < n; i++)
    {
        int num;
        cin >> num;
        if (num < 40)
        {
            arr[i] = num;
        }
        else if (num % 5 == 0)
        {
            arr[i] = num;
        }
        else if ()
    }

    return 0;
}